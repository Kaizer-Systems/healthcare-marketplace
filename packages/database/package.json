{
  "name": "@grip-health/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "seed": "tsx src/seeds/runner.ts",
    "seed:accounts": "tsx src/seeds/runner.ts --accounts-only",
    "db:seed": "tsx src/seeds/runner.ts",
    "db:seed:accounts": "tsx src/seeds/runner.ts --accounts-only"
  },
  "dependencies": {
    "mongodb": "^6.12.0",
    "mongoose": "^8.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0"
  }
}
