# Shared-wildcard cert strategy.
# Ops mount a single wildcard cert at /etc/nginx/certs/wildcard.{crt,key}
# that covers apex + all subdomains (e.g. *.yourdomain.com + yourdomain.com).
ssl_certificate     /etc/nginx/certs/wildcard.crt;
ssl_certificate_key /etc/nginx/certs/wildcard.key;
